import { Text } from 'react-native';
import { Stack, Link } from 'expo-router';

export default function Page() {
  <Stack.Screen
        options={{
          title: "ongoing",
        }}
      />
  return <Text>this is ongiong!</Text>;
}